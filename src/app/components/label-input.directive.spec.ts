import { LabelInputDirective } from './label-input.directive';

describe('LabelInputDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelInputDirective();
    expect(directive).toBeTruthy();
  });
});
